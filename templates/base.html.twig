<!DOCTYPE html>
<html lang="it">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Twitter_X{% endblock %}</title>
        
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        
        {# CSS Esterni (Bootstrap + FontAwesome) #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

        {# Vite (CSS/JS gestiti da Symfony) #}
        {{ vite_entry_link_tags('app') }}

        {% block stylesheets %}
            <style>
                body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; }
                .nav-avatar { width: 32px; height: 32px; object-fit: cover; border-radius: 50%; }
                .navbar { border-bottom: 1px solid #2f3336; }
                .dropdown-menu-dark { background-color: #000; border: 1px solid #2f3336; }
            </style>
        {% endblock %}
    </head>
    <body class="bg-dark text-white {% block body_class %}{% endblock %}">

        <div class="container-fluid d-flex flex-column min-vh-100 p-0">
            <header>
                <nav class="navbar navbar-expand-lg navbar-dark bg-black sticky-top">
                    <div class="container">
                        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
                            <i class="fab fa-twitter fs-4"></i>
                        </a>
                        
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                            <span class="navbar-toggler-icon"></span>
                        </button>

                        <div class="collapse navbar-collapse" id="navbarNav">
                            <ul class="navbar-nav ms-auto align-items-center">
                                {% if app.user %}
                                    <li class="nav-item dropdown">
                                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userMenu" role="button" data-bs-toggle="dropdown">
                                            {% if app.user.userProfile and app.user.userProfile.image %}
                                                <img src="{{ asset('uploads/avatars/' ~ app.user.userProfile.image) }}" alt="Avatar" class="nav-avatar me-2">
                                            {% else %}
                                                <img src="https://ui-avatars.com/api/?name={{ app.user.userProfile.username|default(app.user.email)|url_encode }}&background=0D8ABC&color=fff&size=32" alt="Avatar" class="nav-avatar me-2">
                                            {% endif %}
                                            
                                            <span class="d-none d-md-inline">
                                                {{ app.user.userProfile.username|default(app.user.email) }}
                                            </span>
                                        </a>
                                        <ul class="dropdown-menu dropdown-menu-dark dropdown-menu-end">
                                            <li><a class="dropdown-item" href="{{ path('app_profile', {'user': app.user.id}) }}">Il Mio Profilo</a></li>
                                            <li><a class="dropdown-item" href="{{ path('app_settings_profile') }}">Impostazioni</a></li>
                                            <li><hr class="dropdown-divider"></li>
                                            <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Logout</a></li>
                                        </ul>
                                    </li>
                                {% else %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="{{ path('app_login') }}"><i class="fas fa-sign-in-alt me-1"></i> Login</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="btn btn-light rounded-pill ms-2 px-3 fw-bold" href="{{ path('app_register') }}">Registrati</a>
                                    </li>
                                {% endif %}
                            </ul>
                        </div>
                    </div>
                </nav>
                {% block page_header %}{% endblock %}
            </header>

            <main class="flex-grow-1">
                <div class="container mt-4">
                    {% for label, messages in app.flashes %}
                        {% for message in messages %}
                            <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show" role="alert">
                                {{ message }}
                                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                            </div>
                        {% endfor %}
                    {% endfor %}

                    {% block body %}{% endblock %}
                </div>
            </main>

            <footer class="text-center text-muted py-4 border-top border-secondary mt-auto">
                <small>Twitter Clone &copy; {{ "now"|date("Y") }}</small>
            </footer>
        </div>

        {# Script #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        {{ vite_entry_script_tags('app') }}
        {% block javascripts %}{% endblock %}
    </body>
</html>